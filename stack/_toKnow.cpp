#include <iostream>
#include <stack>
using namespace std;

int main()
{
    stack<char> st;
    cout << "st.pop = "; //<< st.pop() << endl;
    char c = st.pop();
    cout << "st.pop = ";// << st.pop(); 

}